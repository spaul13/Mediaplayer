C:\Users\spauldsnl\AppData\Local\Android\Sdk

Turn off rendering of a terrain
GetComponent(Terrain).enabled = false;

git version control

git init
git add git_readme.txt (from untracked to staged/tracked version control)
git commit -m 'initial'
git clone https://github.com/spaul13/Mediaplayer.git (to clone a existing project)
git commit -a (add and commit after any modification made to the file and have to add commit comments in the notepad++)
git remote add origin https://github.com/spaul13/Mediaplayer.git
git remote -v
output--origin  https://github.com/spaul13/Mediaplayer.git (fetch)
	origin  https://github.com/spaul13/Mediaplayer.git (push)

git push --force <remote repository name>

 
https://git-scm.com/book/en/v2/Git-Basics-Recording-Changes-to-the-Repository
to push changes to the remote repositiory
first (check the status)
1) add
2) commit -> both can be done by (git commit -a)
3) git push --force <remote repository name> (git push origin <branch name> is not working then)





